# 自动 LOD 参数计算 — 产品说明

> 面向产品经理的非技术说明

---

## 这个功能解决什么问题？

3DGS 场景越大，包含的视觉数据越多。渲染时需要控制"同一时刻屏幕上处理多少数据"，否则会导致卡顿。

**以前**：需要开发者手动填两个参数，凭经验调试，场景大小不同参数差异很大，调错了要么卡顿要么画质差。

**现在**：系统根据场景自身的规模，**自动推算**合适的参数——开发者只需告诉系统"希望最多同时渲染多少"，其余交给算法。

---

## 图 1：系统做什么（模块概念图）

```mermaid
graph TD
    subgraph 自动读取["📂 系统自动读取（无需配置）"]
        A["场景空间有多大"]
        B["场景包含多少视觉数据"]
        C["场景支持几个清晰度等级"]
    end

    subgraph 用户配置["🎛️ 用户只需填一项"]
        D["GPU 同时渲染上限\n例如：100 万个数据点"]
        E["场景使用方式\n地面行走 · 航拍俯视 · 线性移动"]
    end

    subgraph 自动计算["🧮 自动推算（后台完成）"]
        F["根据场景规模与性能目标\n推算两个切换距离参数"]
    end

    subgraph 效果["✅ 最终效果"]
        G["近处：最高清晰度"]
        H["越远越简化\n自动均衡画质与性能"]
    end

    自动读取 --> 自动计算
    用户配置 --> 自动计算
    自动计算 --> 效果
```

---

## 图 2：工作流程（使用流程图）

```mermaid
flowchart TD
    Start(["用户打开 3DGS 场景"]) --> Load["场景加载完成\n系统在后台读取场景规模信息"]
    Load --> Config["用户设置性能目标\n「同时最多渲染多少数据点不卡顿」\n默认值已够用，可不修改"]
    Config --> Calc["🧮 系统自动推算\n「清晰层要保持到多远」\n「多远开始降低精细度」"]
    Calc --> Check{效果是否满意？}
    Check --"满意（大多数情况）"--> Done["✅ 直接使用\n无需手动调参"]
    Check --"不满意"--> Adjust["微调性能目标数值\n或手动指定场景类型"]
    Adjust --> Calc
    Done --> Walk(["用户漫游场景\n近处高清 · 远处自动简化 · 始终流畅"])
```

---

## 图 3：场景类型如何影响效果

不同使用方式下，清晰度的切换策略应该不同。用户或开发者可以提前告知系统是哪种使用方式：

```mermaid
graph LR
    Q(["同一个场景\n不同使用方式"]) --> E1 & E2 & E3

    E1["🚶 地面行走\n室内漫游、街道步行\n\n清晰区域较小\n切换比较快\n（因为走动缓慢、视野近）"]

    E2["🚁 航拍俯视\n无人机高空飞行\n\n清晰区域更大\n切换比较平缓\n（因为高空视野宽、需看远处细节）"]

    E3["🚗 线性移动\n公路行驶、河道游览\n\n沿行进方向优化\n横向简化更激进"]
```

---

## 图 4：与手动调参的对比

```mermaid
graph LR
    subgraph 手动["❌ 手动调参（旧方式）"]
        M1["开发者填入\n起始距离 = 10\n递减倍率 = 3"] --> M2["小场景：勉强合适"]
        M1 --> M3["大场景：画质差或性能不稳"]
        M1 --> M4["换场景：重新调一遍"]
    end

    subgraph 自动["✅ 自动计算（新方式）"]
        A1["用户填入\n最大渲染数量 = 100 万"] --> A2["小场景：自动适配"]
        A1 --> A3["大场景：自动适配"]
        A1 --> A4["换场景：无需重新调"]
    end
```

---

## 一句话总结

> 告诉系统「你希望渲染多流畅」,系统自动决定「多远的地方开始降低精细度」。
> 场景越大，切换越早；场景越小，切换越晚。全程无需手动调参。

---

## 计算细节展开（技术人员参考）

### 自动推算内部流程

```mermaid
flowchart TD
    Start([开始自动计算]) --> Phase1

    subgraph Phase1[" 📊 阶段1：采集场景统计数据"]
        A1["遍历场景所有节点"] --> A2["累加：总共多少高斯球数据\n（LOD 0 层的完整数量）"]
        A2 --> A3["合并：所有节点的空间包围盒"]
        A3 --> A4["计算：场景三维尺寸\n（对角线长度）"]
    end

    Phase1 --> Phase2

    subgraph Phase2[" 🧮 阶段2：计算精细层保持距离"]
        B1["读取：用户设定的渲染上限\n例如 100 万"]
        B2["读取：场景类型\n地面行走 / 航拍 / 线性"]
        B1 --> B3["计算密度比：\n渲染上限 ÷ 场景总数据量"]
        B2 --> B3
        B3 --> B4["应用场景类型系数：\n地面行走：立方根缩放\n航拍俯视：平方根缩放\n线性移动：直接比例"]
        B4 --> B5["乘以场景尺寸 =\n推荐的精细层保持距离"]
        B5 --> B6["安全限制：\n最小 1 米 / 最大 200 米"]
    end

    Phase2 --> Phase3

    subgraph Phase3[" 📐 阶段3：计算精细度递减倍率"]
        C1["读取：场景支持几个精细度等级\n例如 4 级"]
        C2["计算：要覆盖的距离跨度\n场景尺寸 ÷ 精细层保持距离"]
        C1 --> C3["按等级数平摊：\n跨度开 N-1 次方根"]
        C2 --> C3
        C3 --> C4["= 推荐的递减倍率\n每级距离放大几倍"]
        C4 --> C5["安全限制：\n最小 2.0 倍 / 最大 5.0 倍"]
    end

    Phase3 --> Output["✅ 输出两个参数\n精细层保持距离 + 递减倍率"]
    Output --> Write["写入渲染器配置\n标记为'已自动计算'"]
```

### 三个关键步骤说明

| 步骤 | 输入 | 计算逻辑 | 输出 | 为什么这样设计 |
|------|------|---------|------|---------------|
| **阶段1：采集** | 场景文件 | 遍历节点统计总数据量和空间尺寸 | 场景大小、数据密度 | 自动适配不同规模场景 |
| **阶段2：保持距离** | 性能目标、场景类型、场景统计 | 根据"希望渲染多少"反推"清晰层该保持到多远" | 精细层保持距离（米） | 场景越大/越密，精细层区域越小 |
| **阶段3：递减倍率** | 精细层距离、场景尺寸、等级数 | 计算几何级数参数，让最粗糙层恰好覆盖整个场景 | 递减倍率（2~5倍） | 保证远处有画面但不消耗性能 |

### 场景类型系数的作用

不同使用方式下，"可见数据量"与"距离"的关系不同：

```mermaid
graph TD
    Type["场景类型决定\n距离增长规律"] --> T1 & T2 & T3

    T1["🚶 地面行走\n三维体积模型\n\n距离扩大 2 倍\n可见范围扩大 8 倍\n→ 用立方根公式"] --> R1["精细层保持距离\n相对较小"]

    T2["🚁 航拍俯视\n二维面积模型\n\n距离扩大 2 倍\n可见范围扩大 4 倍\n→ 用平方根公式"] --> R2["精细层保持距离\n相对较大"]

    T3["🚗 线性移动\n一维长度模型\n\n距离扩大 2 倍\n可见范围扩大 2 倍\n→ 用线性公式"] --> R3["精细层保持距离\n根据主轴优化"]
```

### 安全限制的必要性

| 限制项 | 范围 | 为什么需要 |
|--------|------|-----------|
| 精细层保持距离 | 1 ~ 200 米 | · 太小（<1米）：摄像机刚动就切换，闪烁<br>· 太大（>200米）：整个场景都是最高精度，性能崩溃 |
| 递减倍率 | 2.0 ~ 5.0 倍 | · 太小（<2倍）：等级切换不明显，浪费分级<br>· 太大（>5倍）：远处瞬间变模糊，视觉跳变 |
